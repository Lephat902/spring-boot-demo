spring.config.import=optional:file:.env[.properties]
spring.application.name=demo
spring.datasource.url=${POSTGRES_URL}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=true
spring.jpa.hibernate.naming.physical-strategy=com.phatle.demo.strategy.CustomPhysicalNamingStrategy
springdoc.swagger-ui.path=/api
jwt-key=${SECRET_KEY}
oauth.google.client-id=${GOOGLE_OAUTH_CLIENT_ID}
oauth.google.client-secret=${GOOGLE_OAUTH_CLIENT_SECRET}
oauth.google.redirect.uri=http://localhost:8080/grantcode